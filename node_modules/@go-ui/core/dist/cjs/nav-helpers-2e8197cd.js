'use strict';

const index = require('./index-348d3f4f.js');

function renderIcon(icon) {
    if (!icon) {
        return null;
    }
    if (typeof icon === 'string') {
        return index.h("go-icon", { name: icon });
    }
    return index.h("go-icon", Object.assign({}, icon));
}

exports.renderIcon = renderIcon;

//# sourceMappingURL=nav-helpers-2e8197cd.js.map