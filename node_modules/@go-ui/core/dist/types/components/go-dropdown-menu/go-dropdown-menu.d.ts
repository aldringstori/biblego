import { DropdownProps } from '../../interfaces';
export declare class GoDropdownMenu implements DropdownProps {
    el: HTMLElement;
    /**
     * @see [go-dropdown](go-dropdown#props)
     */
    triggerSelector: string;
    /**
     * keep track of active state
     */
    isActive: boolean;
    /**
     * customise width of dropdown menu
     */
    width?: string;
    /**
     * DOM id for menu
     */
    menuId?: string;
    /**
     * If persistent, the dropdown will not close by itself on interaction with elements inside the menu.
     */
    persistent?: boolean;
    triggerEl: HTMLElement;
    dropdownEl: HTMLGoDropdownElement;
    menuEl: HTMLElement;
    menuItemEls?: HTMLElement[];
    focusedMenuItemIndex: number;
    focusLastOnNextOpen: boolean;
    /**
     * open menu
     * @param focusFirst if auto focus on first item
     */
    open(): Promise<void>;
    close(focusBackToTrigger?: boolean): Promise<void>;
    componentWillLoad(): void;
    componentDidLoad(): void;
    setTriggerRoles(): void;
    addTriggerEventListeners(): void;
    addMenuItemEventListeners(): void;
    focusMenuItem(): void;
    handleDropdownOpened(): void;
    handleDropdownClosed(): void;
    handleItemSelected(): void;
    render(): any;
}
