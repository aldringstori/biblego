import { INavItem } from '../../interfaces';
export interface HeroProps {
    heading: string;
    preHeading?: string;
    breadcrumb?: INavItem[] | string;
    imgSrc?: string;
    imgAlt?: string;
}
/**
 * @slot default - content below the main heading
 */
export declare class GoHero implements HeroProps {
    el: HTMLElement;
    /**
     * Hero heading (h1)
     */
    heading: string;
    /**
     * Pre heading text - use only when required
     */
    preHeading?: string;
    /**
     * Breadcrumb navigation items
     */
    breadcrumbs?: INavItem[] | string;
    /**
     * hero image src url
     * (requires img-alt attribute to be present to render)
     */
    imgSrc?: string;
    /**
     * hero image alt text
     * (requires img-src attribute to be present to render)
     */
    imgAlt?: string;
    /**
     * if we should hide hero img on mobile(`full-width-bg` slot not affected)
     */
    hideImgOnMobile: boolean;
    watchImgSrc(value: string): void;
    watchImgAlt(value: string): void;
    hasFullWidthBg: boolean;
    componentWillLoad(): void;
    render(): any;
}
