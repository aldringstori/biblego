import { EventEmitter } from '../../../stencil-public-runtime';
import { INavItem } from '../../../interfaces';
export declare class GoNavDrawer {
    el: HTMLElement;
    /**
     * Position where the navigation should appear from
     */
    position?: 'left' | 'right';
    /**
     * Navigation items to be rendered
     */
    items?: INavItem[] | string;
    navItems: INavItem[];
    /**
     * keep track of open state of drawer
     */
    active: boolean;
    /**
     * label for screen readers
     */
    label: string;
    /**
     * if true, clicking on nav item will close the drawer automatically
     */
    autoClose: boolean;
    currentSubMenus: HTMLElement[];
    /**
     * Initialise the menu
     * @param items {INavItem[]} menu items to be rendered
     */
    init(newItems: INavItem[] | string): Promise<void>;
    /**
     * Emitted when the nav drawer is opened
     */
    openEvent: EventEmitter<void>;
    open(): Promise<void>;
    /**
     * Emitted when the nav drawer is closed
     */
    closeEvent: EventEmitter<void>;
    /**
     * Emitted when the nav item is clicked
     */
    navItemClickEvent: EventEmitter<INavItem>;
    close(): Promise<void>;
    toggle(): Promise<void>;
    private inheritedAttrs;
    componentWillLoad(): void;
    closeCurrentSubMenu(): Promise<void>;
    private openSubMenu;
    subMenus: {
        string: INavItem[];
    };
    handleNavItemClick(e: any, item: INavItem): void;
    renderNavItems(items: INavItem[], parentItem?: INavItem): any;
    renderNavItem(item: INavItem): any;
    render(): any;
}
