import { INavItem } from "../../../interfaces/index";
import { EventEmitter } from '../../../stencil-public-runtime';
export declare class GoNavItem {
    el: HTMLElement;
    item: INavItem | string;
    submenuId?: string;
    parseItemProp(): void;
    parsedItem: INavItem;
    hasSubmenuSlot: boolean;
    componentWillLoad(): void;
    /**
     * open state of the submenu, only applicable if
     * - the `item` property has `children` key, or
     * - go-nav-item has `submenu` slot
     */
    isOpen: boolean;
    navEvent: EventEmitter;
    subMenuToggleEvent: EventEmitter;
    handleSubmenuToggle(isOpen: boolean): void;
    renderSubMenu(parent: INavItem): any;
    render(): any;
}
