import { h } from "@stencil/core";
export function renderIcon(icon) {
    if (!icon) {
        return null;
    }
    if (typeof icon === 'string') {
        return h("go-icon", { name: icon });
    }
    return h("go-icon", Object.assign({}, icon));
}
//# sourceMappingURL=nav-helpers.js.map
